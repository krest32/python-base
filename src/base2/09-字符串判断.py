if __name__ == '__main__':
    print("测试示例一")
    demoStr = "runoob.com"

    print("判断是否都是数字或者字母：{}".format(demoStr.isalnum()))
    print("判断是否都是字母：{}".format(demoStr.isalpha()))
    print("判断是否都是数字：{}".format(demoStr.isdigit()))
    print("判断是否都是小写：{}".format(demoStr.islower()))
    print("判断是否都是大写：{}".format(demoStr.isupper()))
    print("判断所有单词都是首字母大写，像标题：{}".format(demoStr.istitle()))
    print("判断是否都是字母：{}".format(demoStr.isalpha()))
    print("判断是否都是空白字符如（\\t \\n \\r）：{}".format(demoStr.isspace()))
